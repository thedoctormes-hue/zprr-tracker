[Unit]
Description=Shadowsocks-Rust RF Proxy
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/bin/ssserver -c /etc/shadowsocks-rust/config.json
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target